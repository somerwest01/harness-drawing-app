// Excel utility functions
import * as XLSX from 'xlsx';import { saveAs } from 'file-saver';       saveAs(blob, 'dibujo_guardado.json');    const workbook = XLSX.read(data, { type: 'array' });    const jsonData = XLSX.utils.sheet_to_json(worksheet, { header: 1 });const newWorksheet = XLSX.utils.aoa_to_sheet(updatedSheet);const newWorkbook = XLSX.utils.book_new();XLSX.utils.book_append_sheet(newWorkbook, newWorksheet, sheetName);const excelBuffer = XLSX.write(newWorkbook, { bookType: 'xlsx', type: 'array' });saveAs(blob, 'archivo_con_dimensiones_y_validacion.xlsx');    const worksheet = XLSX.utils.json_to_sheet(exportData);    const workbook = XLSX.utils.book_new();    XLSX.utils.book_append_sheet(workbook, worksheet, 'Líneas');    const excelBuffer = XLSX.write(workbook, { bookType: 'xlsx', type: 'array' });    saveAs(blob, 'resultado_procesado.xlsx');